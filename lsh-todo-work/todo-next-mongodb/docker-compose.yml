version: "3.8"

services:
  mongodb:
    image: docker.m.daocloud.io/library/mongo:7
    container_name: todo-next-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data:
